spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/youssefElhoubi/micro_steam_li
          search-paths: config-service
server:
  port: 8080
